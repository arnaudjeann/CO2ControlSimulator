/**
 * Cette interface permet de déterminer comment mettre à jour les observateurs
 * @author Arnaud Jean
 * @since Hiver 2026
 * @version 1.0
 */
public interface Observer {
    void update(Sensor sensor);
}